// 寻找两个正序数组的中位数

// 给定两个大小分别为 m 和 n 的正序（从小到大）数组 nums1 和 nums2。请你找出并返回这两个正序数组的 中位数 。

function findMedianSortedArrays(nums1: number[], nums2: number[]): number {

  const mid1 = mid % 2 === 0 ? [nums1.length / 2 - 1, nums1.length / 2] Math.ceil(nums1.length / 2) - 1;
  const mid2 = Math.ceil(nums2.length / 2) - 1;

}
